const Discord = require('discord.js');

module.exports = {
    name: "master",
    category: "info",
    permissions: [],
    devOnly: false,
    run: async ({client, message, args}) => {
        const sender = message.author.username

        
    }
}